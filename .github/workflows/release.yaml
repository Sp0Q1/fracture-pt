name: Release
on:
  push:
    tags: ['v*']

jobs:
  release:
    uses: Sp0Q1/fracture-cms/.github/workflows/release-app.yaml@main
    with:
      image_name: ghcr.io/${{ github.repository }}
    secrets: inherit
