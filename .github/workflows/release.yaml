name: Release
on:
  push:
    tags: ['v*']

permissions:
  contents: read
  packages: write

jobs:
  ci:
    uses: ./.github/workflows/rust.yml

  release:
    needs: ci
    uses: Sp0Q1/fracture-cms/.github/workflows/release-app.yaml@433a48e4b910e050c151e38a726310111df0d406 # cms main
    with:
      image_name: ghcr.io/${{ github.repository }}
    # No `secrets: inherit` — the reusable workflow only needs GITHUB_TOKEN,
    # which is provided automatically; inheriting would expose every repo secret.
